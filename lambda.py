# Lambada functions are one lin code functions

def double(x):
    return x * 2

# Creating lambada -- First word is the function name, x is the variable or parameter and then do the aritamatic  
double = lambda x: x * 2

print(double(5))
